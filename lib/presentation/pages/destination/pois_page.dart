import 'package:flutter/material.dart';

class PoisPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Points of Interests')),
      body: Container(),
    );
  }
}
